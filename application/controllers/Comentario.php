<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Comentario extends CI_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Comentario_model');
        $this->load->library('form_validation');
    }

    public function index()
    {
        $q = urldecode($this->input->get('q', TRUE));
        $start = intval($this->input->get('start'));
        
        if ($q <> '') {
            $config['base_url'] = base_url() . 'comentario/index.html?q=' . urlencode($q);
            $config['first_url'] = base_url() . 'comentario/index.html?q=' . urlencode($q);
        } else {
            $config['base_url'] = base_url() . 'comentario/index.html';
            $config['first_url'] = base_url() . 'comentario/index.html';
        }

        $config['per_page'] = 10;
        $config['page_query_string'] = TRUE;
        $config['total_rows'] = $this->Comentario_model->total_rows($q);
        $comentario = $this->Comentario_model->get_limit_data($config['per_page'], $start, $q);

        $this->load->library('pagination');
        $this->pagination->initialize($config);

        $data = array(
            'comentario_data' => $comentario,
            'q' => $q,
            'pagination' => $this->pagination->create_links(),
            'total_rows' => $config['total_rows'],
            'start' => $start,
        );
        $this->load->view('comentario/comentario_list', $data);
    }

    public function read($id) 
    {
        $row = $this->Comentario_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
	    );
            $this->load->view('comentario/comentario_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('comentario'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('comentario/create_action'),
	    'id' => set_value('id'),
	);
        $this->load->view('comentario/comentario_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
	    );

            $this->Comentario_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('comentario'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Comentario_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('comentario/update_action'),
		'id' => set_value('id', $row->id),
	    );
            $this->load->view('comentario/comentario_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('comentario'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
	    );

            $this->Comentario_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('comentario'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Comentario_model->get_by_id($id);

        if ($row) {
            $this->Comentario_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('comentario'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('comentario'));
        }
    }

    public function _rules() 
    {

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Comentario.php */
/* Location: ./application/controllers/Comentario.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2016-12-22 09:56:03 */
/* http://harviacode.com */